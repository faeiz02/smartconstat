# ─── Serveur ───
server.port=8081

# ─── MySQL (XAMPP) ───
spring.datasource.url=jdbc:mysql://localhost:3306/smartconstat_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ─── H2 (Désactivé) ───
# spring.datasource.url=jdbc:h2:file:./data/smartconstat;AUTO_SERVER=TRUE
# spring.datasource.username=sa
# spring.datasource.password=
# spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# ─── JPA / Hibernate ───
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ─── Initialisation données ───
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true

# ─── JWT ───
jwt.secret=SmartConstat2026SecretKeyThatIsAtLeast256BitsLongForHS256Algorithm!!
jwt.expiration=86400000

# ─── CORS ───
cors.allowed-origins=*

# ─── Emails ───
# ─── Emails ───
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=faeiz.kammoun1@gmail.com
spring.mail.password=mkne jqwl zenz ayep
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ─── URLs ───
app.backend.url=http://192.168.1.189:8081
